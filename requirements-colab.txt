# Colab-specific dependencies
# Only packages NOT pre-installed on Google Colab
# Uses relaxed version pins to avoid conflicts with Colab's pre-installed packages
# torch, torchaudio, numpy, scipy, transformers, etc. are provided by Colab

# Core ML
lightning>=2.5.0
ema-pytorch>=0.7.7
torchmetrics>=1.7.0

# Models
muq>=0.1.0
x-transformers>=2.4.14
x-clip>=0.14.4
einx>=0.3.0

# Music structure analysis
msaf>=0.1.80
mir_eval>=0.8.2
jams>=0.3.4

# Audio processing
nnAudio>=0.3.3
julius>=0.2.7
auraloss>=0.4.0
pesq>=0.0.4

# Text processing
ftfy>=6.3.1
pypinyin>=0.54.0
textgrid>=1.6.1
pylrc>=0.1.2

# Training / CLI
schedulefree>=1.4.1
hydra-core>=1.3.2
fire>=0.7.1
loguru>=0.7.3
wandb>=0.19.0

# Utilities
pillow>=11.0.0
scikit-learn>=1.6.0
modelscope>=1.27.0
addict>=2.4.0
blessed>=1.44.0
PyPDF2>=3.0.0
vmo>=0.30.5

# Web UI (not guaranteed in Colab's pre-installed image; needs >=5.0 for
# DownloadButton label/interactive updates used by the batch export tab)
gradio>=5.0
